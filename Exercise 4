cars = 100 # 100 is assigned to the variable "cars". everytime "cars" is called it will print "100"
space_in_a_car = 4.0 # 4.0 is assigned to variable "space_in_a_car". the underscore is the equivalent of *space bar*
drivers = 30 # 30 is assigned to the variable "drivers"
passengers = 90 # 90 is assigned to the variable "passengers"
cars_not_driven = cars - drivers # basically, the cars that are not been driving is calculated by taking the total amount of CARS(100) - DRIVERS(30) since 1 driver = 1 car driven....
cars_driven = drivers # self explanatory, since we need a driver to drive a car therefore since there are 30 "drivers" that means cars_driven is 30. THEREFORE cars_driven = drivers.
carpool_capacity = cars_driven * space_in_a_car
average_passengers_per_car = passengers / cars_driven


print("There are", cars, "cars available.")
print("There are only", drivers, "drivers available.")
print("There will be", cars_not_driven, "empty cars today.")
print("We can transport",carpool_capacity, "people today.")
print("We have",passengers, "to carpool today.")
print("We need to put about", average_passengers_per_car, "in each car") # average amount of passengers to be put in each car *estimate*
